document.addEventListener('DOMContentLoaded', () => {
    const hero = document.querySelector('.hero-bento-grid');
    if (!hero) return;

    // Animations désactivées : la section hero reste statique pour l'accessibilité.
    console.log('Animations désactivées pour la section hero.');
});
